@page
@model Dogs1.Pages.Dog.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Dogs[0].DSL)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Dogs[0].DSN)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Dogs[0].Petss)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Dogs) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DSL)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DSN)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Petss.PSID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.DID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.DID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.DID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
